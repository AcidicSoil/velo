GEMINI_CODE_ASSIST=true
# Enable container-based sandboxing
GEMINI_SANDBOX=docker
CLI_TITLE="velo"
#GEMINI_SANDBOX_PROXY_COMMAND=scripts/example-proxy.js
BUILD_SANDBOX=1

# Or specify podman if you prefer
# GEMINI_SANDBOX=podman

# Set up additional sandbox configurations
#SANDBOX_MOUNTS="../.lmstudio/models:/models:ro,/path/to/data:/data:rw"
#ANDBOX_ENV=VELO_ENV=production,PYTHON_ENV=sandbox

# If you need custom ports exposed
SANDBOX_PORTS=8080,9000

#SANDBOX_ENV="VELO_CONFIG_PATH=/workspace/config,MODEL_PATH=../.lmstudio/models"